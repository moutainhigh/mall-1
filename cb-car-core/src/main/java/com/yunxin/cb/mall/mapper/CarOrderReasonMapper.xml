<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yunxin.cb.mall.dao.CarOrderReasonDao">

<resultMap type="CarOrderReason" id="CarOrderReason">
	<id column="id" property="id" javaType="java.lang.Long" jdbcType="BIGINT"/>
	<result column="order_id" property="orderId" javaType="java.lang.Long" jdbcType="BIGINT"/>
	<result column="reason_type" property="reasonType" javaType="java.lang.Integer" jdbcType="INTEGER"/>
	<result column="reason" property="reason" javaType="java.lang.String" jdbcType="VARCHAR"/>
	<result column="create_time" property="createTime" javaType="java.util.Date" jdbcType="TIMESTAMP"/>
	<result column="operate_id" property="operateId" javaType="java.lang.Integer" jdbcType="INTEGER"/>
	<result column="user_type" property="userType" javaType="java.lang.Integer" jdbcType="INTEGER"/>
</resultMap>


<!-- 全部字段，一般用于明细查询 -->
<sql id="AllColumnlist">
id,order_id,reason_type,reason,create_time,operate_id,user_type

</sql>

<!-- 常见字段，一般用于列表查询，可能不包含备注之类的字段 -->
<sql id="CommonColumnlist">
id,order_id,reason_type,reason,create_time,operate_id,user_type

</sql>

<insert id="add" parameterType="CarOrderReason" useGeneratedKeys="true" keyProperty="id">
	insert into CAR_ORDER_REASON(
		<if test="id!=null">
		id,
		</if>
		order_id,
		reason_type,
		reason,
		create_time,
		operate_id,
		user_type
	) values (
		<if test="id!=null">
		#{id,jdbcType = BIGINT},
		</if>
		#{orderId,jdbcType = BIGINT},
		#{reasonType,jdbcType = INTEGER},
		#{reason,jdbcType = VARCHAR},
		#{createTime,jdbcType = TIMESTAMP},
		#{operateId,jdbcType = INTEGER},
		#{userType,jdbcType = INTEGER}
	)
</insert>

<update id="update" parameterType="CarOrderReason">
	update CAR_ORDER_REASON 
	<set>
		<if test="orderId!=null ">
			order_id=#{orderId,jdbcType = BIGINT},
		</if>
		<if test="reasonType!=null ">
			reason_type=#{reasonType,jdbcType = INTEGER},
		</if>
		<if test="reason!=null and reason!=''">
			reason=#{reason,jdbcType = VARCHAR},
		</if>
		<if test="createTime!=null ">
			create_time=#{createTime,jdbcType = TIMESTAMP},
		</if>
		<if test="operateId!=null ">
			operate_id=#{operateId,jdbcType = INTEGER},
		</if>
		<if test="userType!=null ">
			user_type=#{userType,jdbcType = INTEGER},
		</if>
	</set>
	<where>
		AND id=#{id}
	</where>
</update>

<delete id="delete">
	delete from CAR_ORDER_REASON 
	<where>
		id=#{0}
	</where>
</delete>

<select id="get" resultMap="CarOrderReason">
	select <include refid="AllColumnlist"/> from CAR_ORDER_REASON
	<where>
		id=#{0}
	</where>
</select>

<select id="getByIds" resultMap="CarOrderReason">
	select <include refid="AllColumnlist"/> from CAR_ORDER_REASON
	<where>
		id in <foreach item="item" index="index" collection="array" open="(" separator="," close=")">#{item}</foreach>
	</where>
</select>

<select id="count" parameterType="Query" resultType="Long">
	select count(id) from CAR_ORDER_REASON 
	<where>
	<include refid="listCommonWhere"/>
	</where>
</select>

<select id="queryAll" parameterType="Query" resultMap="CarOrderReason">
	select <include refid="CommonColumnlist"/> from CAR_ORDER_REASON 
	<where>
	<include refid="listCommonWhere"/>
	</where>
</select>

<select id="pageList" parameterType="Query" resultMap="CarOrderReason">
	select <include refid="CommonColumnlist"/> from CAR_ORDER_REASON
	<where>
	 <include refid="listCommonWhere"/>
	</where>
	<![CDATA[LIMIT #{rowIndex},#{pageSize} ]]>
</select>


<sql id="listCommonWhere">
	<if test="q!=null">
		<if test="q.id!=null ">
			AND id=#{q.id}
		</if>
		<if test="q.orderId!=null ">
			AND order_id=#{q.orderId}
		</if>
		<if test="q.reasonType!=null ">
			AND reason_type=#{q.reasonType}
		</if>
		<if test="q.reason!=null and q.reason!=''">
			AND reason=#{q.reason}
		</if>
		<if test="q.createTime!=null ">
			AND create_time=#{q.createTime}
		</if>
		<if test="q.createTimeStart!=null">
		<![CDATA[	AND create_time>=#{q.createTimeStart}  ]]>
		</if>
		<if test="q.createTimeEnd!=null">
		<![CDATA[	AND create_time<#{q.createTimeEnd}   ]]>
		</if>
		<if test="q.operateId!=null ">
			AND operate_id=#{q.operateId}
		</if>
		<if test="q.userType!=null ">
			AND user_type=#{q.userType}
		</if>
	</if>
</sql>

</mapper>