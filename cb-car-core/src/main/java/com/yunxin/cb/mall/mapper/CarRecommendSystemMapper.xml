<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yunxin.cb.mall.dao.CarRecommendSystemDao">

<resultMap type="CarRecommendSystem" id="CarRecommendSystem">
	<id column="id" property="id" javaType="java.lang.Integer" jdbcType="INTEGER"/>
	<result column="system_id" property="systemId" javaType="java.lang.Integer" jdbcType="INTEGER"/>
	<result column="recommend_id" property="recommendId" javaType="java.lang.Integer" jdbcType="INTEGER"/>
	<result column="create_time" property="createTime" javaType="java.util.Date" jdbcType="TIMESTAMP"/>
</resultMap>


<!-- 全部字段，一般用于明细查询 -->
<sql id="AllColumnlist">
id,system_id,recommend_id,create_time
</sql>

<!-- 常见字段，一般用于列表查询，可能不包含备注之类的字段 -->
<sql id="CommonColumnlist">
id,system_id,recommend_id,create_time
</sql>

<insert id="add" parameterType="CarRecommendSystem" useGeneratedKeys="true" keyProperty="id">
	insert into CAR_RECOMMEND_SYSTEM(
		<if test="id!=null">
		id,
		</if>
		system_id,
		recommend_id,
		create_time
	) values (
		<if test="id!=null">
		#{id,jdbcType = INTEGER},
		</if>
		#{systemId,jdbcType = INTEGER},
		#{recommendId,jdbcType = INTEGER},
		#{createTime,jdbcType = TIMESTAMP}
	)
</insert>

<update id="update" parameterType="CarRecommendSystem">
	update CAR_RECOMMEND_SYSTEM 
	<set>
		<if test="systemId!=null ">
			system_id=#{systemId,jdbcType = INTEGER},
		</if>
		<if test="recommendId!=null ">
			recommend_id=#{recommendId,jdbcType = INTEGER},
		</if>
		<if test="createTime!=null ">
			create_time=#{createTime,jdbcType = TIMESTAMP},
		</if>
	</set>
	<where>
		AND id=#{id}
	</where>
</update>

<delete id="delete">
	delete from CAR_RECOMMEND_SYSTEM 
	<where>
		id=#{0}
	</where>
</delete>

<select id="get" resultMap="CarRecommendSystem">
	select <include refid="AllColumnlist"/> from CAR_RECOMMEND_SYSTEM
	<where>
		id=#{0}
	</where>
</select>

<select id="getByIds" resultMap="CarRecommendSystem">
	select <include refid="AllColumnlist"/> from CAR_RECOMMEND_SYSTEM
	<where>
		id in <foreach item="item" index="index" collection="array" open="(" separator="," close=")">#{item}</foreach>
	</where>
</select>

<select id="count" parameterType="Query" resultType="Long">
	select count(id) from CAR_RECOMMEND_SYSTEM 
	<where>
	<include refid="listCommonWhere"/>
	</where>
</select>

<select id="queryAll" parameterType="Query" resultMap="CarRecommendSystem">
	select <include refid="CommonColumnlist"/> from CAR_RECOMMEND_SYSTEM 
	<where>
	<include refid="listCommonWhere"/>
	</where>
</select>

<select id="pageList" parameterType="Query" resultMap="CarRecommendSystem">
	select <include refid="CommonColumnlist"/> from CAR_RECOMMEND_SYSTEM
	<where>
	 <include refid="listCommonWhere"/>
	</where>
	<![CDATA[LIMIT #{rowIndex},#{pageSize} ]]>
</select>


<sql id="listCommonWhere">
	<if test="q!=null">
		<if test="q.id!=null ">
			AND id=#{q.id}
		</if>
		<if test="q.systemId!=null ">
			AND system_id=#{q.systemId}
		</if>
		<if test="q.recommendId!=null ">
			AND recommend_id=#{q.recommendId}
		</if>
		<if test="q.createTime!=null ">
			AND create_time=#{q.createTime}
		</if>
		<if test="q.createTimeStart!=null">
		<![CDATA[	AND create_time>=#{q.createTimeStart}  ]]>
		</if>
		<if test="q.createTimeEnd!=null">
		<![CDATA[	AND create_time<#{q.createTimeEnd}   ]]>
		</if>
	</if>
</sql>

</mapper>