<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yunxin.cb.mall.dao.CarBaseDataDao">

	<update id="enableBaseDataById" parameterType="CarBaseData">
		update CAR_BASE_DATA set enabled=#{enabled} where id=#{baseDataId}
	</update>

	<select id="getObjByNotCode" resultType="long">
		select count(1) from CAR_BASE_DATA
		<where>
			base_data_code=#{baseDataCode} and id != #{id}
		</where>
	</select>

</mapper>