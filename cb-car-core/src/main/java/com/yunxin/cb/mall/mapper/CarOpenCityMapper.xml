<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yunxin.cb.mall.dao.CarOpenCityDao">

<resultMap type="CarOpenCity" id="CarOpenCity">
	<id column="id" property="id" javaType="java.lang.Integer" jdbcType="INTEGER"/>
	<result column="province_name" property="provinceName" javaType="java.lang.String" jdbcType="VARCHAR"/>
	<result column="city_name" property="cityName" javaType="java.lang.String" jdbcType="VARCHAR"/>
	<result column="province_code" property="provinceCode" javaType="java.lang.String" jdbcType="VARCHAR"/>
	<result column="city_code" property="cityCode" javaType="java.lang.String" jdbcType="VARCHAR"/>
	<result column="is_display" property="isDisplay" javaType="java.lang.Boolean" jdbcType="BIT"/>
	<result column="is_hot" property="isHot" javaType="java.lang.Boolean" jdbcType="BIT"/>
	<result column="create_time" property="createTime" javaType="java.util.Date" jdbcType="TIMESTAMP"/>
	<result column="update_time" property="updateTime" javaType="java.util.Date" jdbcType="TIMESTAMP"/>
	<result column="operate_id" property="operateId" javaType="java.lang.Integer" jdbcType="INTEGER"/>
</resultMap>


<!-- 全部字段，一般用于明细查询 -->
<sql id="AllColumnlist">
id,province_name,city_name,province_code,city_code,is_display,is_hot,
create_time,update_time,operate_id
</sql>

<!-- 常见字段，一般用于列表查询，可能不包含备注之类的字段 -->
<sql id="CommonColumnlist">
id,province_name,city_name,province_code,city_code,is_display,is_hot,
create_time,update_time,operate_id
</sql>

<insert id="add" parameterType="CarOpenCity" useGeneratedKeys="true" keyProperty="id">
	insert into CAR_OPEN_CITY(
		<if test="id!=null">
		id,
		</if>
		province_name,
		city_name,
		province_code,
		city_code,
		is_display,
		is_hot,
		create_time,
		update_time,
		operate_id
	) values (
		<if test="id!=null">
		#{id,jdbcType = INTEGER},
		</if>
		#{provinceName,jdbcType = VARCHAR},
		#{cityName,jdbcType = VARCHAR},
		#{provinceCode,jdbcType = VARCHAR},
		#{cityCode,jdbcType = VARCHAR},
		#{isDisplay,jdbcType = BIT},
		#{isHot,jdbcType = BIT},
		#{createTime,jdbcType = TIMESTAMP},
		#{updateTime,jdbcType = TIMESTAMP},
		#{operateId,jdbcType = INTEGER}
	)
</insert>

<update id="update" parameterType="CarOpenCity">
	update CAR_OPEN_CITY 
	<set>
		<if test="provinceName!=null and provinceName!=''">
			province_name=#{provinceName,jdbcType = VARCHAR},
		</if>
		<if test="cityName!=null and cityName!=''">
			city_name=#{cityName,jdbcType = VARCHAR},
		</if>
		<if test="provinceCode!=null and provinceCode!=''">
			province_code=#{provinceCode,jdbcType = VARCHAR},
		</if>
		<if test="cityCode!=null and cityCode!=''">
			city_code=#{cityCode,jdbcType = VARCHAR},
		</if>
		<if test="isDisplay!=null ">
			is_display=#{isDisplay,jdbcType = BIT},
		</if>
		<if test="isHot!=null ">
			is_hot=#{isHot,jdbcType = BIT},
		</if>
		<if test="createTime!=null ">
			create_time=#{createTime,jdbcType = TIMESTAMP},
		</if>
		<if test="updateTime!=null ">
			update_time=#{updateTime,jdbcType = TIMESTAMP},
		</if>
		<if test="operateId!=null ">
			operate_id=#{operateId,jdbcType = INTEGER},
		</if>
	</set>
	<where>
		AND id=#{id}
	</where>
</update>

<delete id="delete">
	delete from CAR_OPEN_CITY 
	<where>
		id=#{0}
	</where>
</delete>

<select id="get" resultMap="CarOpenCity">
	select <include refid="AllColumnlist"/> from CAR_OPEN_CITY
	<where>
		id=#{0}
	</where>
</select>

<select id="getByIds" resultMap="CarOpenCity">
	select <include refid="AllColumnlist"/> from CAR_OPEN_CITY
	<where>
		id in <foreach item="item" index="index" collection="array" open="(" separator="," close=")">#{item}</foreach>
	</where>
</select>

<select id="count" parameterType="Query" resultType="Long">
	select count(id) from CAR_OPEN_CITY 
	<where>
	<include refid="listCommonWhere"/>
	</where>
</select>

<select id="queryAll" parameterType="Query" resultMap="CarOpenCity">
	select <include refid="CommonColumnlist"/> from CAR_OPEN_CITY 
	<where>
	<include refid="listCommonWhere"/>
	</where>
</select>

<select id="pageList" parameterType="Query" resultMap="CarOpenCity">
	select <include refid="CommonColumnlist"/> from CAR_OPEN_CITY
	<where>
	 <include refid="listCommonWhere"/>
	</where>
	<![CDATA[LIMIT #{rowIndex},#{pageSize} ]]>
</select>


<sql id="listCommonWhere">
	<if test="q!=null">
		<if test="q.id!=null ">
			AND id=#{q.id}
		</if>
		<if test="q.provinceName!=null and q.provinceName!=''">
			AND province_name=#{q.provinceName}
		</if>
		<if test="q.cityName!=null and q.cityName!=''">
			AND city_name=#{q.cityName}
		</if>
		<if test="q.provinceCode!=null and q.provinceCode!=''">
			AND province_code=#{q.provinceCode}
		</if>
		<if test="q.cityCode!=null and q.cityCode!=''">
			AND city_code=#{q.cityCode}
		</if>
		<if test="q.isDisplay!=null ">
			AND is_display=#{q.isDisplay}
		</if>
		<if test="q.isHot!=null ">
			AND is_hot=#{q.isHot}
		</if>
		<if test="q.createTime!=null ">
			AND create_time=#{q.createTime}
		</if>
		<if test="q.createTimeStart!=null">
		<![CDATA[	AND create_time>=#{q.createTimeStart}  ]]>
		</if>
		<if test="q.createTimeEnd!=null">
		<![CDATA[	AND create_time<#{q.createTimeEnd}   ]]>
		</if>
		<if test="q.updateTime!=null ">
			AND update_time=#{q.updateTime}
		</if>
		<if test="q.updateTimeStart!=null">
		<![CDATA[	AND update_time>=#{q.updateTimeStart}  ]]>
		</if>
		<if test="q.updateTimeEnd!=null">
		<![CDATA[	AND update_time<#{q.updateTimeEnd}   ]]>
		</if>
		<if test="q.operateId!=null ">
			AND operate_id=#{q.operateId}
		</if>
	</if>
</sql>

</mapper>