<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yunxin.cb.mall.dao.CarHomeBrandDao">

<resultMap type="CarHomeBrand" id="CarHomeBrand">
	<id column="id" property="id" javaType="java.lang.Integer" jdbcType="INTEGER"/>
			<result column="initial" property="initial" javaType="java.lang.String" jdbcType="VARCHAR"/>
			<result column="brand_name" property="brandName" javaType="java.lang.String" jdbcType="VARCHAR"/>
			<result column="is_display" property="isDisplay" javaType="java.lang.Integer" jdbcType="INTEGER"/>
			<result column="create_time" property="createTime" javaType="java.util.Date" jdbcType="TIMESTAMP"/>
			<result column="update_time" property="updateTime" javaType="java.util.Date" jdbcType="TIMESTAMP"/>
</resultMap>


<!-- 全部字段，一般用于明细查询 -->
<sql id="AllColumnlist">
id,initial,brand_name,is_display,create_time,update_time
</sql>

<!-- 常见字段，一般用于列表查询，可能不包含备注之类的字段 -->
<sql id="CommonColumnlist">
id,initial,brand_name,is_display,create_time,update_time
</sql>

<insert id="add" parameterType="CarHomeBrand" useGeneratedKeys="true" keyProperty="id">
	insert into CAR_HOME_BRAND(
		<if test="id!=null">
		id,
		</if>
		initial,
		brand_name,
		is_display,
		create_time,
		update_time
	) values (
		<if test="id!=null">
		#{id,jdbcType = INTEGER},
		</if>
		#{initial,jdbcType = VARCHAR},
		#{brandName,jdbcType = VARCHAR},
		#{isDisplay,jdbcType = INTEGER},
		#{createTime,jdbcType = TIMESTAMP},
		#{updateTime,jdbcType = TIMESTAMP}
	)
</insert>

<update id="update" parameterType="CarHomeBrand">
	update CAR_HOME_BRAND 
	<set>
		<if test="initial!=null and initial!=''">
			initial=#{initial,jdbcType = VARCHAR},

		</if>
		<if test="brandName!=null and brandName!=''">
			brand_name=#{brandName,jdbcType = VARCHAR},

		</if>
		<if test="isDisplay!=null ">
			is_display=#{isDisplay,jdbcType = INTEGER},

		</if>
		<if test="createTime!=null ">
			create_time=#{createTime,jdbcType = TIMESTAMP},

		</if>
		<if test="updateTime!=null ">
			update_time=#{updateTime,jdbcType = TIMESTAMP},

		</if>
	</set>
	<where>
		AND id=#{id}
	</where>
</update>

<delete id="delete">
	delete from CAR_HOME_BRAND 
	<where>
		id=#{0}
	</where>
</delete>

<select id="get" resultMap="CarHomeBrand">
	select <include refid="AllColumnlist"/> from CAR_HOME_BRAND
	<where>
		id=#{0}
	</where>
</select>

<select id="getByIds" resultMap="CarHomeBrand">
	select <include refid="AllColumnlist"/> from CAR_HOME_BRAND
	<where>
		id in <foreach item="item" index="index" collection="array" open="(" separator="," close=")">#{item}</foreach>
	</where>
</select>

<select id="count" parameterType="Query" resultType="Long">
	select count(id) from CAR_HOME_BRAND 
	<where>
	<include refid="listCommonWhere"/>
	</where>
</select>

<select id="queryAll" parameterType="Query" resultMap="CarHomeBrand">
	select <include refid="CommonColumnlist"/> from CAR_HOME_BRAND 
	<where>
	<include refid="listCommonWhere"/>
	</where>
</select>

<select id="pageList" parameterType="Query" resultMap="CarHomeBrand">
	select <include refid="CommonColumnlist"/> from CAR_HOME_BRAND
	<where>
	 <include refid="listCommonWhere"/>
	</where>
	<![CDATA[LIMIT #{rowIndex},#{pageSize} ]]>
</select>


<sql id="listCommonWhere">
	<if test="q!=null">
		<if test="q.id!=null ">
			AND id=#{q.id}
		</if>
		<if test="q.initial!=null and q.initial!=''">
			AND initial=#{q.initial}
		</if>
		<if test="q.brandName!=null and q.brandName!=''">
			AND brand_name=#{q.brandName}
		</if>
		<if test="q.isDisplay!=null ">
			AND is_display=#{q.isDisplay}
		</if>
		<if test="q.createTime!=null ">
			AND create_time=#{q.createTime}
		</if>
		<if test="q.createTimeStart!=null">
		<![CDATA[	AND create_time>=#{q.createTimeStart}  ]]>
		</if>
		<if test="q.createTimeEnd!=null">
		<![CDATA[	AND create_time<#{q.createTimeEnd}   ]]>
		</if>
		<if test="q.updateTime!=null ">
			AND update_time=#{q.updateTime}
		</if>
		<if test="q.updateTimeStart!=null">
		<![CDATA[	AND update_time>=#{q.updateTimeStart}  ]]>
		</if>
		<if test="q.updateTimeEnd!=null">
		<![CDATA[	AND update_time<#{q.updateTimeEnd}   ]]>
		</if>
	</if>
</sql>

</mapper>